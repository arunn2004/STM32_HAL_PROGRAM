# STM32_HAL_PROGRAM
HAL is a software layer provided by MCU vendors (like STMicroelectronics) that lets you control hardware using simple, portable APIs, without touching registers directly. HAL = a library that hides hardware registers and gives easy-to-use functions to control peripherals. write firmware using HAL, use its APIs to access the registers
